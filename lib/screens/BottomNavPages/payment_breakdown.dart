import 'package:flutter/material.dart';

class PaymentBreakdown extends StatelessWidget {
  const PaymentBreakdown({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Payment Breakdown'));
  }
}